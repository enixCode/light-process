# Force LF line endings for all text files, cross-platform.
# This prevents Windows autocrlf from converting files to CRLF on checkout,
# which would break biome lint (biome expects LF).

* text=auto eol=lf

# Explicitly mark binary files so git doesn't try to normalize them
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.tgz binary
